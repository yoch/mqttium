{
  "module": "LifecycleReconnectHook.tla",
  "status": "refinement",
  "summary": "Automatic reconnect proceeds once the on_disconnect hook owns the lifecycle, so a hook awaiting work the replacement connection completes never waits for the reconnect it blocks; on_connect still runs behind it.",
  "issues": [
    508
  ],
  "tests": [
    "tests/unit/test_disconnect_hook_reconnect_progress.py",
    "tests/unit/test_callback_lifecycle_regressions.py"
  ],
  "configs": [
    {
      "config": "LifecycleReconnectHook-rc15.cfg",
      "expect": "deadlock"
    },
    {
      "config": "LifecycleReconnectHook-fixed.cfg",
      "expect": "pass"
    }
  ]
}
