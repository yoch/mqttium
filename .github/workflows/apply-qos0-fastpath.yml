name: Apply QoS0 fast-path patch

on:
  push:
    branches:
      - perf/mqtt5-qos0-direct-decode

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: perf/mqtt5-qos0-direct-decode
      - name: Apply validated patch and remove bootstrap files
        shell: bash
        run: |
          set -euo pipefail
          git apply --check tools/qos0-fastpath.patch
          git apply tools/qos0-fastpath.patch
          rm tools/qos0-fastpath.patch .github/workflows/apply-qos0-fastpath.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Direct-decode MQTT 5 QoS0 publishes'
          git push origin HEAD:perf/mqtt5-qos0-direct-decode
