name: CI

on:
  pull_request:

permissions:
  contents: read

# This file is a disposable placeholder on the temporary benchmark branch.
# Keep the previous temporary concurrency key so a new PR synchronization
# cancels any superseded ARM64 campaign still queued/running from this branch.
concurrency:
  group: temp-scheduler-284-286-arm64
  cancel-in-progress: true

jobs:
  temporary-placeholder:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "ARM64 evidence is produced by temp-scheduler-284-286-arm64.yml"
